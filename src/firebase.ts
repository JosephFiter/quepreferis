// Este archivo es un placeholder para la configuración de Firebase.
// Cuando tengas las credenciales de Firebase, completa este archivo
// y cambia la lógica del GameContext para que use las funciones de Firebase.

// import { initializeApp } from "firebase/app";
// import { getDatabase } from "firebase/database";

const firebaseConfig = {
  apiKey: "PLACEHOLDER_API_KEY",
  authDomain: "PLACEHOLDER_AUTH_DOMAIN",
  databaseURL: "PLACEHOLDER_DATABASE_URL",
  projectId: "PLACEHOLDER_PROJECT_ID",
  storageBucket: "PLACEHOLDER_STORAGE_BUCKET",
  messagingSenderId: "PLACEHOLDER_MESSAGING_SENDER_ID",
  appId: "PLACEHOLDER_APP_ID"
};

// const app = initializeApp(firebaseConfig);
// export const db = getDatabase(app);

export default firebaseConfig;
